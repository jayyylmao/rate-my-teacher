package com.ratemyteacher.entity;

/**
 * Enum representing the type of author for a review.
 * GUEST - Anonymous user, no account
 * USER - Verified email, logged in user
 */
public enum AuthorType {
    GUEST,
    USER
}
